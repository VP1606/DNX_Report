\section{System Architecture Overview}
\label{sec:system_architecture}

To satisfy the success criteria established in Section~\ref{sec:intro_success_criteria} and the functional requirements in Section~\ref{sec:requirements}, the overall system was designed as an end-to-end, highly modular pipeline. Decoupling the preprocessing, neural backbone, and regression stages enabled the isolation and empirical study of individual architectural modifications, ensuring that any performance gap between the spiking and continuous paradigms is attributable to the paradigm swap itself rather than to differences in preprocessing or head design. The high-level data flow is formalized in Figure~\ref{fig:system_architecture_block}.

\begin{figure}[htbp]
    \centering
    \includegraphics[height=0.85\textheight, keepaspectratio]{chapter_3/figures/high-level-architecture.pdf}
    \caption[High-level system architecture pipeline.]{The high-level system architecture pipeline, illustrating the flow from raw ETraM high-definition event data, through the fixed \gls{bolt} preprocessing stages, into the interchangeable neural backbone (SNN or CNN), and concluding with the multi-box regression head and the comprehensive evaluation suite.}
    \label{fig:system_architecture_block}
\end{figure}

As illustrated, the architecture is divided into four primary sequential stages, where the central neural backbone acts as an interchangeable module:

\begin{enumerate}
    \item \textbf{Data Ingestion \& The \gls{bolt} Pipeline (Fixed):}
    Raw, asynchronous event data at a high spatial resolution of $1280 \times 720$ is ingested from the \gls{etram} dataset.
    Due to the sparse and noisy nature of neuromorphic hardware outputs, the data enters the \gls{bolt} preprocessing pipeline, which is heavily adapted from the methodology proposed in the original \gls{etram} study \cite{Verma_2024_CVPR}.
    While the core spatiotemporal noise filtering and bilinear temporal interpolation (to $T=10$ bins) remain faithful to the original study, this project included an additional spatial downsampling stage ($ds=4: 1280 \times 720 \rightarrow 320 \times 180$).
    This modification was strategically implemented to ensure training feasibility on local accelerator hardware and reduce input sparsity, thereby improving the stability of neuronal feature extraction.
    This stage is detailed thoroughly in Section \ref{sec:data_preprocessing}.

    \begin{sloppypar}
    \item \textbf{The Neural Backbone (Interchangeable):}
    The preprocessed voxel tensor serves as the input to the primary feature extraction engine.
    This stage houses either the spiking architecture \texttt{DetectorNX-G3-SNN} or its parameter-matched continuous counterpart \texttt{DetectorNX-G3-CNN}.
    Both backbones comprise four residual blocks --- \texttt{SEWResBlock} units in the SNN variant, Standard ResBlocks in the CNN baseline --- with Squeeze-and-Excitation attention at the Stage~3 bottleneck, and together perform an aggressive $8\times$ total spatial downsampling.
    The evolution and formal design of these backbones are detailed in Sections~\ref{sec:snn_architecture} and~\ref{sec:cnn_baseline}.
    \end{sloppypar}

    \item \textbf{Multi-Box Regression Head (Fixed):} 
    The final abstracted feature maps from either backbone are fed into a unified detection head. 
    To resolve the spatial ambiguities and ``ghost detections'' observed in earlier architectural iterations, the head utilises an $8 \times 8$ Adaptive Pooling mechanism to aggregate features from 64 spatial receptive blocks.
    This abstracted information is then mapped to a final output tensor of shape $[B, 5, 5]$, representing the top 5 predicted bounding boxes per image. 
    Each detection is defined by the feature set $\mathcal{F} = \{x, y, w, h, c\}$, where $(x, y, w, h)$ denote the normalised bounding box coordinates and $c$ represents the object confidence logit.
    The regression output is evaluated via a fully vectorised Multi-Box Loss function during training.

    \item \textbf{Evaluation \& Benchmarking Suite (Fixed):}
    The predictions and internal neuronal activity from both backbones are passed into a dedicated benchmarking suite that pairs each of the six success criteria with a matched experiment, as summarised in Table~\ref{tab:sc_traceability}. Diagnostic analyses of quantisation error (Section~\ref{subsec:quantization_error_setup}) \cite{cordone2022objectdetectionspikingneural} and temporal evidence accumulation (Section~\ref{subsec:temporal_evidence}) complement the primary accuracy result by explaining \textit{how} the \gls{snn}'s rate-coded regression converges, rather than validating a distinct criterion. The full benchmarking suite is detailed in Section~\ref{sec:experimental_setup}.

\end{enumerate}

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{|c|p{4.5cm}|p{5cm}|p{4.5cm}|}
\hline
\textbf{SC} & \textbf{Success Criterion} & \textbf{Architectural Mechanism} & \textbf{Validating Experiment} \\
\hline
SC1 & Detection accuracy parity ($\geq 90\%$ \gls{miou}) & Multi-Box regression head, \texttt{SEWResBlock} backbone, SE attention at Stage~3 bottleneck & Global Evaluator (Section~\ref{subsec:global_evaluator}) \\
\hline
SC2 & Measurable theoretical energy savings & Binary spike encoding, \gls{frr} metabolic penalty & Dynamic Power (Section~\ref{subsec:dynamic_power}) \& Theoretical Energy (Section~\ref{subsec:energy_consumption}) \\
\hline
SC3 & Real-time viability ($\geq 30$ \gls{fps}) & Fused CUDA kernels (\texttt{step\_mode='m'}), \gls{amp} 16-bit tensor cores & Real-Time Latency (Section~\ref{subsec:exp_latency_setup}) \\
\hline
SC4 & Architectural parity (within $1\%$ parameters) & 1:1 block-for-block backbone matching, shared regression head, shared \gls{bolt} input & Parameter count audit (Tables \ref{tab_app:g3_snn_params} and \ref{tab_app:g3_cnn_params}) \\
\hline
SC5 & Detection completeness ($100\%$ recall @ \gls{iou}$>0.5$) & Vectorised Multi-Box Loss with greedy assignment matching & Global Evaluator (Section~\ref{subsec:global_evaluator}) \\
\hline
SC6 & Confidence calibration equivalence & Objectness logits $+$ \texttt{BCEWithLogitsLoss} on unmatched boxes & Confidence Calibration (Section~\ref{subsec:confidence_calibration}) \\
\hline
\end{tabular}%
}
\caption[Success criteria traceability table.]{Mapping of each success criterion (Section~\ref{sec:intro_success_criteria}) to the architectural mechanism that addresses it and the dedicated experiment that validates it in Chapter~\ref{cha:summary_conclusions}.}
\label{tab:sc_traceability}
\end{table}

This Fixed/Interchangeable partition is the mechanism by which SC4 (Architectural Parity) is enforced: all paradigm-level variance between the two evaluated models is confined to Stage~2, so any discrepancy observed in Chapter~\ref{cha:summary_conclusions} is attributable to the spike-vs-continuous swap alone.