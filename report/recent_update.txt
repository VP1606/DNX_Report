=== TTFS / ROC removal from §2.3.3 (word-count reduction) ===

Goal: cut the TTFS (Time-to-First-Spike) and ROC (Rank Order Coding)
alternative-encoding paragraph from §2.3.3 (subsec:bg_temporal) as part
of the drive to bring total word count below 17k. The project uses
rate coding exclusively, so alternative coding schemes are out of
scope for Background; they properly belong in Future Work, where
they are already introduced.

Files touched:
  - report/chapter_2/2_3_snn.tex

Edits applied:

1. chapter_2/2_3_snn.tex
   - Deleted the entire "Alternative temporal coding schemes have been
     proposed to overcome this ceiling..." sentence (previously line
     57) which defined \acrfull{ttfs} and \acrfull{roc} inline.
   - Retained the preceding "quantisation error / 1/T ceiling" sentence
     (line 56), which anchors the §3.8 quantization_error experiment.

Savings: ~42 texcount words.

Safety checks (verified before applying):
  - \gls{ttfs} and \gls{roc} are still used in
    chapter_4/reflection_future.tex lines 34-35, where their first-use
    expansion will now occur. Acronym definitions in
    abbreviations.tex:33-34 are untouched, so \gls auto-expansion still
    works correctly.
  - ttfs_encoding bibkey remains cited at reflection_future.tex:34, so
    it does not become orphaned.
  - THORPE2001715 is still cited on the retained line 56, so the
    citation remains live.
  - No \ref{} or \cite{} becomes broken.

Rubric fit:
  - Tightens the Background narrative to the coding scheme the project
    actually uses (rate coding), removing a tangent the examiner does
    not need.
  - Alternative encodings now live exclusively in the Future Work
    section, where they are more natural as proposed directions.
